# Optional. Never put a real key in client-side code.
# Server-only. Used by /api/chat and /api/realtime/session.
OPENAI_API_KEY=
# Optional. Defaults to gpt-4o-mini for text chat.
# OPENAI_MODEL=gpt-4o-mini
# Optional Realtime voice model. Defaults to gpt-realtime.
# OPENAI_REALTIME_MODEL=gpt-realtime
# Optional Realtime voice name. Defaults to marin.
# OPENAI_REALTIME_VOICE=marin
